import {createStore} from 'vuex'

import TasksModule from './module/tasks-module'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    TasksModule
  }
})
